import { FileConstant, OUTPUT_PATH } from './types'

const FILENAME = 'HistoryStatistics.json'

export const HISTORY_STATISTICS: FileConstant = {
  name: FILENAME,
  path: `${OUTPUT_PATH}/${FILENAME}`,
}
