import { FileConstant, OUTPUT_PATH } from './types'

const FILENAME = 'HighlightedAuctions.json'

export const HIGHLIGHTED_AUCTIONS: FileConstant = {
  name: FILENAME,
  path: `${OUTPUT_PATH}/${FILENAME}`,
}
