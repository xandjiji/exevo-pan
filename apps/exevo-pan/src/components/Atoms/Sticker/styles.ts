import styled from 'styled-components'

export const Wrapper = styled.span`
  padding: 3px;
  background-color: var(--battleYellow);
  border-radius: 6px;
  border: dashed 1px var(--separator);

  font-size: 9px;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  color: #000;

  opacity: 0.8;
`
