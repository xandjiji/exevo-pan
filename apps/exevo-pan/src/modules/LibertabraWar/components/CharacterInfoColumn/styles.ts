import styled from 'styled-components'
import { Table as BaseTable } from 'components/Atoms'

export const CharacterColumn = styled(BaseTable.Column)``

export const CharacterInfo = styled.span`
  margin-top: 6px;
  display: block;
  font-size: 10px;
`
