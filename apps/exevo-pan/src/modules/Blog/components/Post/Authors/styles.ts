import styled from 'styled-components'
import BaseBlockSection from '../BlockSection'

export const BlockSection = styled(BaseBlockSection)`
  padding: 0;
  background-color: transparent;
  box-shadow: none;
`

export const Title = styled.h5`
  margin-bottom: 16px;
  font-size: 19px;
  font-weight: 300;
  line-height: 1.3;
  letter-spacing: 0.5px;
`
