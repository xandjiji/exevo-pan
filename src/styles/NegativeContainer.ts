import { css } from 'styled-components'

export const NegativeContainer = css`
  margin-left: -20px;
  margin-right: -20px;

  @media (min-width: 768px) {
    margin-left: -40px;
    margin-right: -40px;
  }
`
