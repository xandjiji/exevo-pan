import styled from 'styled-components';

export default styled.header`
    padding-top: 8px;
    padding-bottom: 4px;
    margin-bottom: 12px;
    background-color: var(--primary);

    svg {
        border-radius: 4px;
        fill: var(--onPrimary);
    }
`;