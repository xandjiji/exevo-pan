import styled from 'styled-components';

export default styled.div`
    padding: 8px;
    border-radius: 5px;
    background-color: var(--primaryVariant);
    user-select: none;

    img {
        margin-top: -24px;
        margin-left: -24px;
        user-select: none;
    }
`;