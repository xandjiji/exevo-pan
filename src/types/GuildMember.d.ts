declare type MiniGuildMember = [string, number, number, number]

declare type GuildMember = {
  nickname: string
  vocation: string
  vocationId: number
  deathCount: number
  kills: number
  guild: string
  guildId: number
}
