import { randomDataset } from 'utils/test'

const { miniPuneMembersData, puneMembersData } = randomDataset()

export const mockedMiniPuneMembers = miniPuneMembersData
export const mockedPuneMembers = puneMembersData
