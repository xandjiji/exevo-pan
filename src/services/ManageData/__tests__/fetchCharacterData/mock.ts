import { randomDataset } from 'utils/test'

const { minifiedCharacterData, partialCharacterData } = randomDataset()

export const mockedMinifiedData = minifiedCharacterData
export const mockedBuildedData = partialCharacterData
