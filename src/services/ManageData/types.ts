export type ServerDataResponse = Record<string, ServerObject>

export type MinifiedCharacterObject = [
  number,
  string,
  number,
  number,
  boolean,
  string,
  number,
  number,
  number,
]

export type RareItemData = Record<string, RareItemObject>
