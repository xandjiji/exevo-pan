export type KsuData = {
  characters: {
    data: {
      name: string
      vocation: string
      level: number
      world: string
    }
  }
}

export type SectionProps = {
  title: string
  id: string
}
